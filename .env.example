# Copy this file to .env and fill in real values.

# Host serving the reference IQM Parquet files used by the
# "Dataset + reference" IQM viewer mode. Required only when that mode
# needs to download data for the first time (already-cached modalities
# under .streamlit/reference_cache/ work without it).
REFERENCE_DATA_URL=
