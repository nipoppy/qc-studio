"""Full-screen view modules (landing, congratulations) imported by ``app.py``.

Multipage sidebar scripts live under ``pages/`` and call into these modules
with the same CLI context as ``main.py``.
"""
